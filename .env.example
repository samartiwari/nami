# Copy this file to .env and fill in real values:  cp .env.example .env
# These are the database settings the app and docker-compose both read.

DB_HOST=localhost
DB_PORT=5433
DB_NAME=nami
DB_USER=nami
DB_PASSWORD=changeme
